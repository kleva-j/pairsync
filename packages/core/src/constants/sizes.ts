/** Chunk size for streaming transfers, bytes (4MB). */
export const CHUNK_SIZE = 4 * 1024 * 1024;

/** Mobile receive buffer limit, bytes (50MB). */
export const MOBILE_BUFFER_LIMIT = 50 * 1024 * 1024;

/** Desktop receive buffer limit, bytes (200MB). */
export const DESKTOP_BUFFER_LIMIT = 200 * 1024 * 1024;
